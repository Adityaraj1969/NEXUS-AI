/**
 * NEXUS AI — Test Setup
 *
 * Configures the testing environment for Vitest + React Testing Library.
 * Loaded before each test file via vitest.config.js `setupFiles`.
 */
import '@testing-library/jest-dom';
